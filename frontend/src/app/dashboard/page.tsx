import { DashboardShell } from "@/components/dashboard/dashboard-shell";

/** Dashboard — data from `cput-api` (port 8787) via Next.js proxy. */
export default function DashboardPage() {
  return <DashboardShell />;
}
